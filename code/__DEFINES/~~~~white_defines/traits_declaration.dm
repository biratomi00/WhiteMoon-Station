#define TRAIT_JUMPER "jumper"
/// Only Admins can heal you. NOTHING else does it unless it's given the god tag. Right now it only works for heal_damage(). For AdjustXXXLoss() use COMSIG_LIVING_ADJUST_STANDARD_DAMAGE_TYPES and signals.
#define TRAIT_NONATURALHEAL "nonaturalheal"
